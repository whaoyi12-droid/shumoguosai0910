# C 题项目工作约定

## 适用范围与选题
- 本文件适用于 `C题/` 及其全部子目录，沿用上级工作约定和审批红线。
- 已确定选题：2026 年全国大学生数学建模竞赛 C 题《微网与外部电网电力调控策略》。未经用户明确要求，不得改变选题、转做其他题目或替换研究目标。
- 以 `C题.pdf`、原始附件及经核实的官方更正为任务依据。不得跳脱题目要求范围，不擅自增加研究任务、约束、目标函数或交付要求。
- 为回答题目所必需的假设必须明确标注并说明依据，不能冒充题设。发现题面、附件或模板不一致时，记录证据；影响结果且无法合理确定的问题交由用户确认。

## 比赛顺序
严格按以下顺序推进：
1. 选择选题：C（已完成）。
2. 进行数学分析。
3. 确定数学模型。
4. 求解模型。
5. 模型验证和优化。
6. 总结成果进入论文，论文由人工智能辅助撰写。

- 不跳过数学分析或模型确定而直接堆砌算法、生成结果或撰写定论。
- 数据核对、实现检查和结果核验在对应阶段内开展；验证发现问题时可返回相关阶段修正，并说明原因。
- 如需增加其他必要流程，先向用户说明用途、影响和插入位置，征询并获得用户同意后再加入；不得自行扩展上述流程。
- 本文件的建立不代表已经完成数学分析、确定模型或获得全部后续工作的执行指令。

## AI 协作与审查责任
- 本题使用 Chat GPT6 辅助完成，由 GPT 主 agent 负责统筹、关键判断与最终审查。此处记录用户指定的协作安排，不据此虚构实际运行模型身份。
- 允许调用 Claude Code（用户说明已接入 DeepSeek 4.1 Flash）和本地 Ornith 小模型，辅助完成简单、边界明确、可核验的任务，例如格式整理、字段清点、机械性代码片段或文字初稿。
- 调用前核实工具实际可用及模型配置；不可用时如实说明，不擅自更改接入配置、安装全局依赖或替换为其他服务。
- 委派时明确输入、输出、可访问路径、改动范围和验收标准，并提供完成任务所需的最少资料。
- 数学建模、代码撰写、结果审查等关键工作必须由 GPT 亲自审查：核对假设与推导、阅读实际代码、检查运行证据与结果。不能仅依据辅助模型的摘要或“已通过”声明接受成果。
- 辅助模型不得自行确定或改变选题、核心模型、评价口径和最终结论；未经 GPT 审查的产物不得作为定稿或已验证结果。

## 目录与命名
- `C题.pdf`、`附件/`：题面、原始数据与原始结果模板，保留原貌；生成结果不得覆盖原始文件。
- `src/`：模型、求解与数据处理代码；文件、函数和变量使用清晰的英文命名，Python 文件采用 `snake_case`。
- `results/`：经计算生成的表格及题目要求的结果文件；提交文件名遵循题面模板要求。
- `docs/`：数学分析、模型说明、审查记录及后续论文材料。
- `docs/STATUS.md`：长任务的状态记录，简记当前阶段、已确认模型与假设、已完成事项、验证证据、待解决问题和下一步。
- `figures/`：仅在符合下述图片保存条件时使用。
- 上述目录按需建立，不为填充目录生成占位代码、数据或论文。

## 代码、结果与验证
- 2026-09-10 用户确认的周期口径：以附件中当天 `0:10` 至次日 `0:00` 的 144 个十分钟记录为一个周期，按原始顺序编号，不添加额外的 `0:00` 数据点。记录时间解释为该十分钟段的结束时刻，周期初始储电量在当天 `0:00`，末状态在次日 `0:00`；模板文字差异单独记录，不能按错位文字平移原始数据。
- 从题目目标、变量、约束和可用信息出发选择最简可验证模型；不得为使用某种算法而改变问题。
- 区分原始数据、预测值、仿真值和实际求解结果，不伪造附件数据、运行记录、文献或模型优势。
- 对 C 题重点核验：kW 与 kWh 换算、10 分钟时段对应、储能容量与充放电功率限制、效率口径、能量平衡、跨日储电量衔接，以及计划、调整和紧急购电的费用核算。
- 日前与滚动决策只使用该决策时刻可获得的信息。使用未来实际值的理想基准必须明确标注，不得冒充可执行策略。
- 保存足以复现结果的参数、输入对应关系和运行方法；随机过程记录种子。结论须能追溯到模型、代码与结果。
- 按改动运行相称的检查，GPT 审查实际产物。明确区分已运行验证、静态检查和未验证内容，不以程序成功退出替代模型有效性验证。

## 图片展示与论文
- 在用户将论文发来之前，代码默认不加入图片保存逻辑，不批量导出图像；以数值、表格和临时显示图片的方式展示结果。
- 有重要信息需要留存时，允许保存必要图片，并说明保存原因及其对应的结果或证据。此例外不构成默认保存所有图片的授权。
- 上述限制针对图片，不影响题目要求的结果表格和必要复现记录的保存。
- 论文由人工智能辅助撰写，内容必须与已审查的模型、实现和实际结果一致，不提前编造数值、结论或验证效果。
- 最终论文美化阶段使用适用的 Nature skills：按具体任务选用 nature-polishing、nature-figure 等，并在调用前读取相应技能说明。仍以中文数模论文、用户要求及当届竞赛格式为准，不擅自改为英文或 Nature 投稿格式。
- 论文发来后的配图与排版按用户当次要求实施；Nature skills 不得扩大选题范围、增加未经同意的流程或改写已验证的科学结论。
